class Employee:
    def __init__(self, employeenumber, firstname, lastname, address, city, state, zip)
        self.EmployeeNumber = employeenumber.strip()
        self.FirstName = firstname.strip()
        self.LastName = lastname.strip()
        self.Address = address.strip()
        self.City = city.strip()
        self.State = state.strip()
        self.Zip = zip.strip()

class EmployeeList:
    def __init__(self, filename)
        self.EmployeeList = []
        self.FileName = filename.strip()

    def ReadEmployeeFile():

    def WriteEmplyeeFile():

    def DisplayEmployeeList():

    def ReadEmployee(EmployeeNumber):

    def AddEmployee(firstName, lastName, address, city, state, zip):

    def UpdateEmployee(employeenumber, firstName, lastName, address, city, state, zip):

    def DeleteEmployee(employeenumber):

    def FindEmployee(EmployeeNumber):

    def NextEmployeeNumber():
